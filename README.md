# Log Generator – Python

Simple application Python that generates simulated logs
(INFO, WARNING, ERROR) at regular intervals.

## Configuration
Environment variables:
- SERVICE_NAME
- LOG_INTERVAL
- LOG_LEVEL
- LOG_FILE

## Run locally
```bash
python app/app.py

test ci